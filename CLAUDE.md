# Ride Prep

## Testing

After every code change, run the tests to validate the code works:

```
node --test tests/tests.node.js
```

There is also a browser-based test suite at `tests/tests.html` for visual verification.
